# Super-Resolution Models

Store Real-ESRGAN weight files under `models/super_resolution/weights/`.

Use `scripts/setup_super_resolution_weights.py` to copy or download weights into the
expected locations without committing binaries to git.
